require 'spec_helper'

describe FlightDeal do
  it 'has a version number' do
    expect(FlightDeal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
